# redis.conf
save 60 1                  # Save the dataset to disk every 60 seconds if at least 1 write occurred
dir /data                  # Specify the data directory for persistence
appendonly yes             # Enable AOF persistence (append-only file)
appendfilename "appendonly.aof"  # Specify AOF file name
